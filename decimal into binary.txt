#include <stdio.h>
#include <math.h>
int main() {
  int num , binary=0 ,base=1, rem ;
  printf("enter a decimal no.");
  scanf("%d",&num);
  while(num>0){
      rem =num% 2;
      binary = binary +rem * base ;
      num=num/2 ;
      base = base *10 ;
  }
  printf("binary no=%d\n", binary);
    return 0;
}